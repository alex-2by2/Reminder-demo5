{
  "name": "reminder-demo5-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Render-ready backend API for Reminder Demo 5 with Firebase Auth owner-only admin access and optional MongoDB persistence.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.1.0",
    "helmet": "^7.1.0",
    "mongodb": "^6.8.0",
    "morgan": "^1.10.0"
  }
}
