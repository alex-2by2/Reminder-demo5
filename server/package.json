{
  "name": "master-reminder-admin-server",
  "version": "1.0.0",
  "private": true,
  "description": "Owner-only admin backend for the Master Reminder App — reads/writes the same Firestore project the app uses, via the Firebase Admin SDK. Not used by regular users; the app itself keeps talking to Firebase directly, unchanged.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18.17"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "firebase-admin": "^12.1.0",
    "helmet": "^7.1.0"
  }
}
